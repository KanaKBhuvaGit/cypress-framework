const PATH_PREFIX = "api/"

const ENDPOINTS = {
    user: PATH_PREFIX + 'user',
    users: PATH_PREFIX + 'users/2',
    register: PATH_PREFIX + 'register',
    login: PATH_PREFIX + 'login',
};

export default ENDPOINTS;